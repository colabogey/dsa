#pragma once
#include <string>

class Anagram {
public:
    Anagram() = default;
    ~Anagram() = default;
    bool isAnagramSort(std::string&, std::string&);
    bool isAnagramUnorderedMap(std::string&, std::string&);

private:

};
